{
  "name": "@trust-leaf/zk-circom",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:circuit": "circom ./circuits/prescription.circom --r1cs --wasm --sym -o ./build",
    "prepare:input": "node ./scripts/prepare-input.mjs",
    "witness": "node ./build/prescription_js/generate_witness.js ./build/prescription_js/prescription.wasm ./build/input.json ./build/witness.wtns",
    "pack:proof": "node ./scripts/pack-proof.mjs",
    "prove": "echo \"TODO: snarkjs groth16 prove\""
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.6"
  }
}
